#!/bin/bash

# This script starts the full application (frontend + backend)
# Designed for Replit's workflow system

# Start the application using npx for consistent execution
exec npx tsx server/index.ts